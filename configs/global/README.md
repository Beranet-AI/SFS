# Global Configuration

Use this directory for shared configuration artifacts (environment templates, shared settings, secrets examples) that are consumed by multiple services. Keep secrets out of version control.
