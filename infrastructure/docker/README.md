# Docker Assets

This folder is reserved for Docker Compose stacks and related artifacts. The existing Compose file at `infra/docker-compose.yml` remains in place for backward compatibility; copy or move it here when you are ready to consolidate infrastructure assets.
