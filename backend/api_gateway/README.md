# API Gateway Alias

The active API gateway implementation lives in `backend/services/api_gateway/`. This alias directory exists to align with the proposed repository layout and to host gateway-specific configs or docs without relocating code.
