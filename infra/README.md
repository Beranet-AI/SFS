# Infra (legacy root)

This directory now only serves as a placeholder for shared environment files (for example, `.env.global`) referenced by `infrastructure/docker/docker-compose.yml`. Compose files have moved to `infrastructure/docker/`.
